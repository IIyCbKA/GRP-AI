/*
--------------LoadStatus enum--------------
The enum LoadStatus specifies status of load of data.
By default, status is IDLE.
*/
export const enum LoadStatus {
  IDLE,
  LOADING,
  SUCCEEDED,
  FAILED,
}
